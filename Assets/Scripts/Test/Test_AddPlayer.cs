using UnityEngine;
using XiaoCao;

namespace Assets.Scripts.Test
{
    public class Test_AddPlayer : MonoBehaviour
    {
        private void Update()
        {
            //if (Input.GetKeyDown(KeyCode.F1))
            //{
            //    var player = PlayerManager.Instance.LocalPlayer;
            //    Vector3 forward = player.transform.forward;
            //    forward.y = 0;
            //    PlayerManager.Instance.AddFakePlayer(player.transform.position + forward * 5,true);
            //}

            ////需要的功能 (AI开关)
            //if (Input.GetKeyDown(KeyCode.F2))
            //{

            //}
        }

    }
}
